import { useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import Today from './Today';

const Index = () => {
  // The index page now shows the Today dashboard
  return <Today />;
};

export default Index;
